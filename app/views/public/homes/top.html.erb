<div class="jumbotron top">
  <div class="container">
    <div class="row">
      <div class="welcome-message_top">
        <p id="welcome-text_top">誰かと食べるケーキ。<br>
        ひとりで食べるケーキ。<br>
        どっちも幸せ。<br>
        甘い幸せを、ながのcakeがお届けします。
        </p>
      </div>
    </div>
  </div>
</div>
<div class="container">
  <div class="row">
      <div class="pickup-item mt-1">
        <h3 class="new_arrival">New</h3>
        <div>
          <% @active_items.each.with_index(1) do |item| %>
            <div class="item-box">
              <div>
                <%= link_to item_path(item) do %>
                <%= attachment_image_tag item, :image, :fill, 200, 150, fallback: "noimage.jpg" %>
                <% end %>
              </div>
              <div class="item_name"><%= item.name %></div>
              <p>¥<%= (item.price * 1.1).round %>(税込)</p>
            </div>
          <% end %>
        </div >
        <div>
        <%= link_to "View all", items_path, :style=>"color: #343a40;" %>
        </div>
      </div>
    </div>
  </div>
</div>
</div>
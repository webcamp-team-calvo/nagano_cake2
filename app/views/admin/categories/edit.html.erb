<div class="container">
  <div class="row">
	 <div class="col-lg-">

    <h5 class="bg-light">ジャンル編集</h5>
      <%= form_with model: [:admin, @category], local:true do |f| %>
        <table class="table">
          <tr>
           <th>ジャンル名</th>
           <th><%= f.text_field :name %></th>
           <th><%= f.submit "変更を保存", class: "btn btn-success"%></th>
          </tr>
        </table>
     <% end %>
    </div>
   </div>
</div>